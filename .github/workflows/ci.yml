name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Audit dependencies
        run: npm audit

      - name: Check documentation and committed results
        run: npm run check

      - name: Install parity browser
        run: npx playwright install --with-deps chromium

      - name: Test equivalent behavior
        run: npm test

      - name: Reproduce current-source application profiles
        run: npm run benchmark:route-split && npm run benchmark:hand-authored && npm run benchmark:optimization-sensitivity

      - name: Verify normalized measurements
        run: npm run verify

      - name: Rebuild charts
        run: npm run charts

      - name: Verify deterministic charts
        run: git diff --exit-code -- docs/images
