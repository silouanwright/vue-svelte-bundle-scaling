/**
 * Dashboard project-card dimensions — shared by the grid cards, the stack
 * fan layout math, and the drag-overlay clone.
 */
export const PROJECT_CARD_WIDTH = 220;
export const PROJECT_CARD_HEIGHT = 180;
